<!DOCTYPE html>
<html>
  <head>
    <title>AFF STUDENT LOGBOOK</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="container">
    <%= render 'layouts/header' %>

    <% if notice || alert %>
      <div class="flex justify-center">
        <% if notice %>
          <p class="border-2 border-green-600 w-fit py-3.5 px-3.5 mb-5 rounded-md"><%= notice %></p>
        <% end %>
        <% if alert %>
          <p class="border-2 border-red-600 w-fit py-3.5 px-3.5 mb-5 rounded-md"><%= alert %></p>
        <% end %>
      </div>
    <% end %>

    <%= yield %>

    <%= render 'layouts/footer' %>
  </body>
</html>
